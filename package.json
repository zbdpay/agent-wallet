{
  "name": "@axobot/cli",
  "version": "1.2.0",
  "description": "CLI wallet and command router for Axo agent payments",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zbdpay/axobot-cli.git"
  },
  "bugs": {
    "url": "https://github.com/zbdpay/axobot-cli/issues"
  },
  "homepage": "https://github.com/zbdpay/axobot-cli#readme",
  "keywords": [
    "axo",
    "axobot",
    "wallet",
    "lightning",
    "cli"
  ],
  "type": "module",
  "bin": {
    "axo": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "npm run typecheck",
    "release": "semantic-release",
    "release:dry-run": "SEMANTIC_RELEASE_DRY_RUN=true semantic-release --dry-run --no-ci",
    "smoke:cli": "npm run build && node ./test/smoke/cli-smoke.mjs",
    "test": "npm run build && node --test test/**/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@axobot/fetch": "file:../axobot-fetch",
    "@axobot/mppx": "file:../mppx",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.6",
    "@semantic-release/npm": "^13.1.4",
    "@semantic-release/release-notes-generator": "^14.1.0",
    "@types/node": "^22.13.10",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "semantic-release": "^25.0.0",
    "typescript": "^5.8.2"
  }
}
